resources:
- ../../base
- ../../../components/service-api/overlays/dev
- ../../../components/apicurio-registry/overlays/dev
# - (additional resources that will only be applied in this environment)

# patches to resources in this environment
namespace: service-registry-staging

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

